# Bacphlip Service Configuration

# Input/Output settings
data_dir: "data"
output_dir: "output"

# Bacphlip settings
hmmer_threads: 4
cleanup_intermediate: true

# Server settings
server:
  host: "0.0.0.0"
  port: 8003
