[project]
name = "phabox-service"
version = "0.1.0"
description = "FastAPI wrapper around PhaBOX2 for phage taxonomy, lifestyle, and host prediction"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.100",
    "uvicorn>=0.22",
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "loguru>=0.7",
]
